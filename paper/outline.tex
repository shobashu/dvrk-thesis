% Paper outline for review, per Allison's format:
%   - title
%   - authors
%   - complete abstract (placeholders OK for unknown numbers)
%   - outline down to subsection level
%   - all expected figures/tables and captions (placeholder/sketch OK)
% This is a separate deliverable from the thesis (main.tex / sections/).
% It mirrors the thesis structure but is condensed for review, and
% marks open items as [TODO] / PLACEHOLDER rather than writing full
% prose -- the point is to align on scope and structure before the
% thesis content is finalized.
\documentclass[11pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{cite}
\usepackage{hyperref}

\title{Real-Time Multimodal Performance Assessment for dVRK \\
\large Paper Outline -- Draft for Review}

% TODO (with the group): confirm author order and affiliations.
% Placeholder order below: student first, mentors, co-supervisor,
% supervisor/corresponding last -- standard convention, not yet
% confirmed with anyone listed.
\author{
  Shujiro Shobayashi$^1$ \and
  Brian Vuong$^{1}$ \and % TODO: confirm affiliation
  Mary Kate Gale$^{1}$ \and % TODO: confirm affiliation
  Josephine Hughes$^2$ \and
  Allison M. Okamura$^{1}$ \\[2mm]
  \normalsize $^1$Stanford University \quad $^2$EPFL
}
\date{\today}

\begin{document}
\maketitle

\begin{center}
\fbox{\parbox{0.9\textwidth}{\centering
\textbf{Status:} Outline for review -- structure and scope, not final
content. Items marked \textbf{[TODO]} or \textbf{PLACEHOLDER} are open;
everything else reflects what is already drafted in the thesis.}}
\end{center}

\section*{Abstract}
% Complete structurally; numeric results are placeholders pending data.
Real-time performance feedback can accelerate skill acquisition during
robotic surgical training, but existing systems either compute
performance measures only after a trial ends, or depend on a human
rater to trigger and score feedback during the trial. We present a
multimodal system that measures peg-transfer task performance live, on
a physical da Vinci Research Kit (dVRK), with no human rater at any
point in the loop. The system fuses kinematic, visual, and force data
through an event-triggered pipeline: trial time and instrument path
length are tracked in real time, cylinder orientation is checked at
placement via HSV segmentation on a YOLOv8-detected cylinder, and
contact force is reported immediately after each trial. A
higher-accuracy, SAM2-based 3D localization method runs post-hoc as an
offline validation reference for the real-time measures, not as a
delivered real-time measure itself. Across [$N$ participants, $N$
trials -- PLACEHOLDER], the real-time measures agree with the post-hoc
reference to within [PLACEHOLDER], with per-trial feedback latency of
[PLACEHOLDER]. These results show that automated, real-time, on-robot
performance measurement is feasible for a structured surgical training
task, and lay the groundwork for a closed-loop, difficulty-adaptive
training system.

\section*{Outline}

\subsection*{1. Introduction}
\begin{itemize}
  \item \textbf{1.1 Scope} -- Measures performance on one well-defined
  task (peg transfer), not global surgical skill; task-level metrics
  are objective and real-time-computable, a global skill judgment is
  neither.
  \item \textbf{1.2 Goal} -- Real-time dVRK peg-transfer monitoring,
  computed directly from sensor/vision data, no human rater. Four
  measures tracked: completion time, path length, angular displacement,
  object state. Long-term vision: closed-loop, difficulty-adaptive task.
  \item \textbf{1.3 Motivation} -- (a) real-time feedback accelerates
  learning~\cite{laca2022}; (b) automated capture catches events a
  human observer misses; (c) multimodal data can surface metrics beyond
  completion time.
  \item \textbf{1.4 Contribution} -- Closes the gap between offline
  accurate measurement and real-time-but-human-scored feedback: a
  multimodal, automated system, live, on the physical robot, for one
  task.
\end{itemize}

\subsection*{2. Related Work}
\begin{itemize}
  \item \textbf{2.1 Post-Hoc Kinematic Analysis} -- JIGSAWS-style
  skill proxies (path length, velocity, jerk) computed from recorded
  kinematics~\cite{jigsaws}; accurate but offline.
  \item \textbf{2.2 Post-Hoc Vision} -- Object detection on recorded
  video for task-progress tracking; accurate but too heavy for
  real-time.
  \item \textbf{2.3 Real-Time Multimodal Skill Prediction} -- Closest
  technical precedent (Hu et al.~\cite{hu2026}): kinematic + vision
  fusion, real-time-capable model, but evaluated offline on recorded
  trials and predicts a general OSATS score, not task-specific metrics.
  \item \textbf{2.4 Real-Time Feedback Attempts} -- Closest in spirit
  (Laca et al.~\cite{laca2022}): real-time feedback RCT on physical
  hardware, but the performance measure behind the feedback is scored
  post-hoc by blinded human raters.
  \item \textbf{2.5 The Gap} -- No prior approach is simultaneously
  automated, real-time, and on physical hardware. \emph{[Table 1 --
  see Expected Figures/Tables below]}
\end{itemize}

\subsection*{3. System \& Task Setup}
\begin{itemize}
  \item \textbf{3.1 Hardware} -- dVRK: da Vinci S console, da Vinci Si
  patient-side arms, bipolar fenestrated forceps, PSM1/PSM2.
  \item \textbf{3.2 Task} -- Cylinder-on-peg transfer: 4 center pegs
  (rest position), 8 outer pegs with blue/white LEDs (target
  indication), conductive-paint board for ground-truth contact
  detection. \emph{[Figure 1 -- task board]}
  \item \textbf{3.3 Vision} -- dVRK endoscopic stereo camera; Intel
  RealSense D405 depth camera, fixed external view.
  \item \textbf{3.4 Sensing} -- PSM kinematics; Arduino-based
  task start/end trigger; ATI force sensor under the task pad.
\end{itemize}

\subsection*{4. Methods}
\begin{itemize}
  \item \textbf{4.1 Pipeline Overview} -- Integrated pipeline, not
  independent scripts: lift $\to$ Arduino signal $\to$ clock starts;
  place $\to$ Arduino signal $\to$ clock stops $\to$ HSV orientation
  check; end of trial $\to$ force graph displayed.
  \emph{[Figure 2 -- pipeline diagram; Table 2 -- data/timing/feedback
  summary]}
  \item \textbf{4.2 Real-Time GUI \& Trial Clock} -- Arduino-triggered
  start/stop; path length accrues while clock runs. \emph{[Figure 3 --
  GUI screenshot]} [TODO: source code reference/appendix]
  \item \textbf{4.3 Object Detection} -- YOLOv8, 4 classes (cylinder,
  inactive/blue/white peg); backbone for 4.5--4.6. [TODO: training
  data, accuracy]
  \item \textbf{4.4 Path Length} -- [TODO: computation method --
  kinematic tool-tip pose vs. vision-tracked cylinder]
  \item \textbf{4.5 Angular Displacement} -- HSV segmentation on
  YOLOv8-detected cylinder crop; orientation estimate at placement.
  [TODO: color thresholds / angle derivation]
  \item \textbf{4.6 Object State Detection} -- Correct/incorrect
  placement scoring from 4.5 against the lit target peg; separately,
  a 4-class state classifier (stationary/held/dropped/lost) augmented
  with XGBoost. [TODO: classifier details, training data]
  \item \textbf{4.7 Ground Contact Detection} -- ATI force sensor;
  displayed as a post-trial 2D graph (time vs.\ force in N), not
  streamed live. \emph{[Figure 4 -- force graph]}
  \item \textbf{4.8 3D Localization (Post-Hoc)} -- SAM2-based cylinder
  segmentation from the D405 depth stream, $(x,y,z)$ over the trial.
  Not real-time (SAM2 runtime exceeds the trial budget); serves as an
  offline validation reference for 4.4/4.5, not a delivered real-time
  measure. Early real-time attempt was unreliable [TODO: failure mode].
  \item \textbf{4.9 Experimental Protocol} -- [TODO: participants,
  trial structure, number of runs per participant]
\end{itemize}

\subsection*{5. Results \& Evaluation -- PLACEHOLDER}
% Not yet written in the thesis; proposed structure for review.
\begin{itemize}
  \item \textbf{5.1 Participants \& Trials} -- [TODO: $N$, demographics
  if any, trial counts]
  \item \textbf{5.2 Real-Time Measure Accuracy} -- Real-time path
  length / angular displacement / object state validated against the
  post-hoc SAM2 reference (4.8). \emph{[Figure 5 -- accuracy/agreement
  plot]}
  \item \textbf{5.3 Timing \& Latency} -- End-to-end feedback latency
  per measure. \emph{[Figure 6 -- latency plot]}
  \item \textbf{5.4 [Open]} -- [TODO: skill-level comparison? novice
  vs.\ expert? decide with group]
\end{itemize}

\subsection*{6. Discussion -- PLACEHOLDER}
[TODO: interpretation of results; limitations, incl. the real-time vs.
post-hoc 3D localization tradeoff (4.8); what the negative result on
the early real-time attempt implies]

\subsection*{7. Conclusion \& Future Work}
\begin{itemize}
  \item Summary of contribution (real-time, automated, on-robot,
  no human rater, one task).
  \item Outlook: closed-loop, difficulty-adaptive task (farther peg,
  tighter orientation tolerance, difficulty set by prior trial score).
\end{itemize}

\section*{Expected Figures and Tables}
% Placeholders and sketches per Allison's format. Real assets noted
% where they already exist in figures/ or pictures/.

\begin{figure}[h]
\centering
\fbox{\parbox[c][3cm]{0.6\textwidth}{\centering PLACEHOLDER\\
photo/diagram of task board -- exists as a commented placeholder in
\texttt{sections/system\_design.tex}, image not yet captured}}
\caption{Task board layout: four center pegs hold the cylinders at
rest; eight outer pegs illuminate blue or white via embedded LEDs to
indicate the target cylinder and destination for the next move.}
\label{fig:outline_task_board}
\end{figure}

\begin{figure}[h]
\centering
\fbox{\parbox[c][3cm]{0.6\textwidth}{\centering PLACEHOLDER -- SKETCH\\
block diagram: PSM kinematics / endoscope / D405 / Arduino / ATI force
sensor $\to$ pipeline $\to$ GUI}}
\caption{System and data pipeline architecture: sensing and vision
inputs, the Arduino-triggered event pipeline, and the real-time GUI
outputs (Section~4.1).}
\label{fig:outline_pipeline}
\end{figure}

\begin{figure}[h]
\centering
\fbox{\parbox[c][3cm]{0.6\textwidth}{\centering PLACEHOLDER\\
screenshot of the real-time GUI (trial clock, path length, orientation
indicator)}}
\caption{Real-time GUI during a trial, showing the trial clock, running
path length, and the placement orientation indicator.}
\label{fig:outline_gui}
\end{figure}

\begin{figure}[h]
\centering
\fbox{\parbox[c][3cm]{0.6\textwidth}{\centering PLACEHOLDER\\
example post-trial force graph (time vs.\ newtons)}}
\caption{Contact force applied to the task pad over the course of one
trial, as displayed to the trainee after the trial ends.}
\label{fig:outline_force}
\end{figure}

\begin{figure}[h]
\centering
\fbox{\parbox[c][3cm]{0.6\textwidth}{\centering PLACEHOLDER -- pending
data\\ real-time measures vs.\ post-hoc SAM2 reference}}
\caption{Agreement between real-time path length / angular displacement
and the post-hoc SAM2-based 3D localization reference.}
\label{fig:outline_accuracy}
\end{figure}

\begin{figure}[h]
\centering
\fbox{\parbox[c][3cm]{0.6\textwidth}{\centering PLACEHOLDER -- pending
data\\ per-measure feedback latency}}
\caption{End-to-end feedback latency for each real-time measure.}
\label{fig:outline_latency}
\end{figure}

\noindent\textbf{Table 1} -- Related work comparison (automated /
real-time / on physical hardware), already drafted as
\texttt{tab:related\_work} in \texttt{sections/background.tex}; to be
reused here.

\noindent\textbf{Table 2} -- Data collected, timing class (real-time /
delayed / post-hoc), trigger, and feedback form, already drafted as
\texttt{tab:data\_timing} in \texttt{sections/methods.tex}; to be
reused here.

\bibliographystyle{plain}
\bibliography{../references}

\end{document}
